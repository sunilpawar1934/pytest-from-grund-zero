# pytest-from-grund-zero

# Note on virtualev
Checkout how if you run `pip freeze | wc -l` there are many package you may not want

1. `virtual ~/.ven`
2. `vim ~/.bashrc` and put in `source ~/.ven/bin/activate`
